import { Layout } from "antd";
import styled from "styled-components";

const { Footer } = Layout;

export const FooterWrapper = styled(Footer)`
                text-align: center;
                margin-top: auto;
                width: 100%;
`